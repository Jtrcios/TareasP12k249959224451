#include <iostream>
using namespace std;

int main() {
    int potencia = 1;

    // Ciclo while infinito
    while (true) {
        cout << potencia << endl;
        potencia *= 2; // Multiplicar la potencia por 2 en cada iteración de la ejeución
    }

    return 0;
}

